{
  "name": "job-search-engine",
  "version": "1.0.0",
  "description": "Job Search Automation Engine — aggregates job listings from ATS platforms, scores them, and sends Telegram alerts",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun run --watch src/index.ts",
    "ingest": "bun run src/scripts/ingest.ts",
    "digest": "bun run src/scripts/digest.ts",
    "backfill": "bun run src/scripts/backfill.ts",
    "discover-boards": "bun run src/scripts/discover-boards.ts",
    "health-check": "bun run src/scripts/health-check.ts",
    "status": "bun run src/scripts/status.ts",
    "cleanup-expired": "bun run src/scripts/cleanup-expired.ts",
    "retry-alerts": "bun run src/scripts/retry-alerts.ts",
    "archive-old-jobs": "bun run src/scripts/archive-old-jobs.ts",
    "test-dry-run": "DRY_RUN=true bun run src/index.ts",
    "replay": "bun run src/scripts/replay.ts"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "fuse.js": "^7.1.0",
    "hono": "^4.11.9",
    "node-cron": "^4.2.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "@types/node-cron": "^3.0.11"
  }
}
